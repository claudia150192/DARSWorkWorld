require 'spec_helper'

describe UserSessionsController do
  describe "POST /user_sessions" do
    it "sets the cookie if the 'remember me' block is checked" do
    end

    it "does not set the cookie if the 'remember me' block is not checked" do
    end
  end
end
