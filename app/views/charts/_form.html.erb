<%= form_for(@chart) do |f| %>
  <% if @chart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chart.errors.count, "error") %> prohibited this chart from being saved:</h2>

      <ul>
      <% @chart.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :chart_type %><br />
    <%= f.select(:chart_type, Chart::TYPES) %>
  </div>
  <div class='field'>
    <%= f.label :stack_type %><br/>
    <%= f.select(:stack_type, Chart::STACK_TYPES) %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :yaxis_label %><br />
    <%= f.text_field :yaxis_label %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
