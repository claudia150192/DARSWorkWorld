<% title "Editing medicaid annual threshold" %>

<% form_for(@medicaid_annual_threshold) do |f| %>
  <%= f.error_messages %>
	<%= render :partial => "form", :object => f %>
  <%= f.submit "Update", :class => "button" %> <%= link_to 'Cancel', :back %>
<% end %>
