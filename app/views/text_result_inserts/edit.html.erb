<% title "Edit Text Result Inserts" %>

<% form_for(@text_result_insert) do |f| %>
  <%= f.error_messages %>
	<%= render :partial => "form", :object => f %>
  <p>
    <%= f.submit 'Update', :class => "button" %> <%= link_to 'Cancel', :back %>
  </p>
<% end %>
