<%= form_for(@chart_result) do |f| %>
  <% if @chart_result.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chart_result.errors.count, "error") %> prohibited this chart_result from being saved:</h2>

      <ul>
      <% @chart_result.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :web_calc %><br />
    <%= f.collection_select(:web_calc_id, WebCalc.find(:all, :order=>'name'), :id,:name) %>
  </div>
  <div class="field">
    <%= f.label :chart %><br />
    <%= f.collection_select(:chart_id, Chart.all,:id,:title) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
