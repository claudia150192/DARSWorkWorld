<% title "New web numerical results group" %>

<%= form_for(@web_numerical_results_group) do |f| %>
  <%= f.error_messages %>
	<%= render :partial => "form", :object => f %>
  <p>
    <%= f.submit 'Create', :class => "button" %> <%= link_to 'Cancel', :back %> %>
  </p>
<% end %>
