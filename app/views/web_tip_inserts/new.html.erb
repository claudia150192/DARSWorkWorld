<% title "Create Web Tip Insertion" %>

<% form_for(@web_tip_insert) do |f| %>
  <% @web_tip_insert.errors.full_messages.each do |msg| %>
	<p><%= msg %></p>
  <% end %>
	<%= render :partial => "form", :object => f %>
  <p>
    <%= f.submit 'Create', :class => "button" %> <%= link_to 'Cancel', :back %>
  </p>
<% end %>
