<% title "Editing medicaid annual blind threshold" %>

<% form_for(@medicaid_annual_blind_threshold) do |f| %>
  <%= f.error_messages %>
	<%= render :partial => "form", :object => f %>
  <%= f.submit "Update", :class => "button" %> <%= link_to 'Cancel', :back %>
<% end %>
